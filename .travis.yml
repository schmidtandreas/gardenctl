language: c
sudo: required
compiler:
  - gcc
dist: xenial
before_install:
  - sudo apt update -q

addons:
  apt:
    packages:
      - autoconf
      - automake
      - libmosquitto-dev
      - libconfuse-dev
      - uncrustify

script:
  - ./autogen.sh
  - ./configure
  - make
  - git diff
  - ./uncrustify.sh && git diff --exit-code
